export const metadata = {
  title: "Blog | LETSTOP",
  description: "Explore articles, expert insights, and industry updates from LETSTOP and its global partners.",
}

import BlogClient from "./BlogClient"

export default function BlogPage() {
  return <BlogClient />
}